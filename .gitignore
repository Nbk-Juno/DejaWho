node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Environment / secrets
.env
.env.*
!.env.example

# Claude Code local overrides + working state
.claude/

# Editor / OS
.vscode/
.idea/
*.swp
*.swo

# Test artifacts
test-results/

# Playwright MCP (browser session state, screenshots, console logs)
.playwright-mcp/

# Impeccable design-skill local state (design.json sidecar + critique snapshots —
# regenerable, and trend tracking is personal per contributor)
.impeccable/

# AI tooling installer artifacts (.agents mirrors .claude/skills for other agents;
# skills-lock.json pins skill versions for the same toolchain)
.agents/
skills-lock.json
