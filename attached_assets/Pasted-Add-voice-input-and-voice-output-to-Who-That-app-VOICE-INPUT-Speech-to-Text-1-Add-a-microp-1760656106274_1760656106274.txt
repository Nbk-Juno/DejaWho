Add voice input and voice output to "Who That!?" app:

VOICE INPUT (Speech-to-Text):
1. Add a microphone button on both "Record Encounter" and "Search" pages
2. When clicked, record audio from user's microphone
3. Send audio to OpenAI Whisper API for transcription
4. Auto-fill the form fields (for recording) or search box (for searching)
5. Show recording indicator (red dot, waveform animation)
6. Stop recording after 60 seconds max or when user clicks stop

VOICE OUTPUT (Text-to-Speech):
1. ONLY activate when user used voice input (don't speak for text queries)
2. After getting search results, convert the naturalLanguageResponse to speech
3. Use ElevenLabs API for high-quality voice (if available)
4. If ElevenLabs not available, use OpenAI TTS API as fallback
5. Auto-play the response
6. Add speaker icon to show audio is playing
7. Allow user to pause/replay audio

TECHNICAL REQUIREMENTS:

Voice Input Implementation:
- Use browser MediaRecorder API to capture audio
- Convert to format Whisper accepts (MP3 or WAV)
- Add endpoint: POST /api/transcribe
- Use OpenAI Whisper API: whisper-1 model
- Handle microphone permissions gracefully

Voice Output Implementation:
- Add endpoint: POST /api/text-to-speech
- Use ElevenLabs API (voice: "Rachel" or "Adam")
- If ElevenLabs unavailable, use OpenAI TTS (voice: "alloy" or "nova")
- Return audio as base64 or stream
- Auto-play on frontend using Audio API

UI CHANGES:

Record Encounter Page:
- Add microphone button next to form
- "Tap to speak your encounter details"
- When recording: show pulsing red indicator
- After transcription: parse and fill name, location, context fields

Search Page:
- Add microphone button in/next to search box
- "Ask me anything..."
- Show recording animation
- After transcription: auto-submit search
- Play back results as voice response

UX REQUIREMENTS:
- Smooth animations for recording states
- Clear visual feedback (recording, processing, playing)
- Error handling: no mic access, transcription failed
- Loading states: "Listening...", "Transcribing...", "Thinking..."
- Don't play voice for text-based queries (only for voice queries)

ENVIRONMENT VARIABLES:
- OPENAI_API_KEY (already exists)
- ELEVENLABS_API_KEY (add this)

SAMPLE CODE STRUCTURE:

Backend endpoint for transcription:
```typescript
app.post('/api/transcribe', async (req, res) => {
  const audioFile = req.file; // audio from frontend
  
  const transcription = await openai.audio.transcriptions.create({
    file: audioFile,
    model: "whisper-1"
  });
  
  res.json({ text: transcription.text });
});
```

Backend endpoint for TTS:
```typescript
app.post('/api/text-to-speech', async (req, res) => {
  const { text } = req.body;
  
  // Try ElevenLabs first
  try {
    const audio = await elevenlabs.textToSpeech({
      text: text,
      voice_id: "21m00Tcm4TlvDq8ikWAM" // Rachel
    });
    res.send(audio);
  } catch {
    // Fallback to OpenAI TTS
    const mp3 = await openai.audio.speech.create({
      model: "tts-1",
      voice: "alloy",
      input: text,
    });
    res.send(mp3);
  }
});
```

Frontend voice recording:
- Use MediaRecorder Web API
- Show visual feedback during recording
- Send blob to /api/transcribe
- Fill form or search box with result

Frontend audio playback:
- Receive audio from /api/text-to-speech
- Create Audio element
- Auto-play response
- Show speaker icon with pause/replay controls

CRITICAL: Only use voice output when user used voice input. Track this with a flag:
```typescript
const [isVoiceQuery, setIsVoiceQuery] = useState(false);
// Set to true when mic button used
// Only play TTS if isVoiceQuery === true
```

Make the voice interaction feel seamless and natural. The goal is:
1. User taps mic → speaks
2. App transcribes → processes
3. App speaks back answer

All in under 5 seconds for the full flow.