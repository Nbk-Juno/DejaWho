# @react-email/* declare strict peer ranges that npm 7+ rejects against our
# pinned react/react-dom even though they satisfy the range. legacy-peer-deps
# keeps `npm install` reproducible locally, in CI, and on Render (whose build
# runs a plain `npm install --include=dev`).
legacy-peer-deps=true
